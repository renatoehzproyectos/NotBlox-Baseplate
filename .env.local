# Development (slug is appended in code, so this hits one local backend on 8001)
NEXT_PUBLIC_SERVER_URL=ws://localhost:8001

# Production example (set in Vercel):
# NEXT_PUBLIC_SERVER_URL=wss://your-backend.example.com
